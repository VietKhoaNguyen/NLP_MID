# Vietnamese Hate Speech Detection

This project detects Vietnamese hate speech using:

- TF-IDF + SVM baseline
- PhoBERT transformer model

## Demo

Streamlit UI:

streamlit run app_streamlit.py

## Install

pip install -r requirements.txt

## Train model

python scripts/train_svm.py